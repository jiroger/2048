//this is in its own class because multiple classes use this method
public class Utility_Functions {
	public static boolean isBetween(int n, int lower, int upper) {
		return (n >= lower && n <= upper);
	}
}
